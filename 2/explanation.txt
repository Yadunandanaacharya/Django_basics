We'll see 2 djangoprojects, inside one is base app of our project, it's like 
root app.

manage.py you won't change anything in that.

Always remember and check to set path and run server Always

set path command : cd
to go to previous directory: cd ..
cls : clear screen 

To get out of the server press ctrl+c

When we will run python manage.py runserver
we will get db.sqlite3, django project will configure with sqlite